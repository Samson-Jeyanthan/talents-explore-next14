import { TPostProps } from "@/types/post.types";

type Props = {
  postData: TPostProps;
};
const MediaCarosel = ({ postData }: Props) => {
  return <div>MediaCarosel</div>;
};

export default MediaCarosel;
